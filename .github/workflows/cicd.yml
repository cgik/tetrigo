name: cicd
on:
  push:
    branches:
      - july-game-update-v1
    tags:
      - 'v*'

concurrency:
  group: ${{ github.ref }}

jobs:
  build:
    if: github.ref == 'refs/heads/july-game-update-v1'
    uses: ./.github/workflows/build.yml
    with:
      app: tetrigo-web
    secrets: inherit

  deploy:
    if: startsWith(github.event.ref, 'refs/tags/v')
    uses: ./.github/workflows/deploy.yml
    with:
      app: tetrigo-web
    secrets: inherit
